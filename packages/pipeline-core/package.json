{
  "name": "@ai-video/pipeline-core",
  "version": "0.0.0",
  "private": true,
  "description": "Generic compilation pipeline engine (stage registry, runner, retry policies). NOTE: CIR shims and render impl shims in src/cir/ and src/providers/video/ still reference @ai-video/pipeline-video — full decoupling is tracked as future work once those files migrate.",
  "dependencies": {
    "@ai-video/lib": "*",
    "@ai-video/pipeline-video": "*",
    "@ai-video/shared": "*"
  },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "test": "vitest run --dir src"
  }
}
