{
  "name": "@ai-video/pipeline-core",
  "version": "0.0.0",
  "private": true,
  "description": "Generic compilation pipeline engine (stage registry, runner, retry policies, CIR types). NOTE: render impl shims in src/providers/video/ still reference @ai-video/pipeline-video — full render decoupling is tracked as future work.",
  "dependencies": {
    "@ai-video/lib": "*",
    "@ai-video/pipeline-video": "*",
    "@ai-video/shared": "*"
  },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "test": "vitest run --dir src"
  }
}
